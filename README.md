# robolectric-runner-bug

Run all tests: `./gradlew testDebugUnitTest`

Run single failed test (and it works!): `./gradlew testDebugUnitTest --tests ExampleUnitTest1`

Downgrade robolectric to 3.8 and it works again